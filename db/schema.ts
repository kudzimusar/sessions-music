import {sql} from 'drizzle-orm';
import {sqliteTable,text,integer,primaryKey,uniqueIndex,index,check} from 'drizzle-orm/sqlite-core';
export const workspaces=sqliteTable('workspaces',{owner:text('owner').primaryKey(),content:text('content').notNull()});
export const rooms=sqliteTable('rooms',{owner:text('owner').notNull(),id:text('id').notNull(),content:text('content').notNull()},t=>[primaryKey({columns:[t.owner,t.id]})]);
export const bookings=sqliteTable('bookings',{owner:text('owner').notNull(),id:text('id').notNull(),requestKey:text('request_key').notNull(),content:text('content').notNull()},t=>[primaryKey({columns:[t.owner,t.id]}),uniqueIndex('booking_request').on(t.owner,t.requestKey)]);
export const claims=sqliteTable('slot_claims',{owner:text('owner').notNull(),roomId:text('room_id').notNull(),date:text('date').notNull(),minute:integer('minute').notNull(),bookingId:text('booking_id').notNull()},t=>[primaryKey({columns:[t.owner,t.roomId,t.date,t.minute]})]);
export const blocks=sqliteTable('blocks',{owner:text('owner').notNull(),id:text('id').notNull(),content:text('content').notNull()},t=>[primaryKey({columns:[t.owner,t.id]})]);
export const uploads=sqliteTable('uploads',{id:text('id').primaryKey(),owner:text('owner').notNull(),type:text('type').notNull(),studioId:text('studio_id'),roomId:text('room_id'),bookingId:text('booking_id'),purpose:text('purpose'),createdAt:text('created_at')},t=>[index('uploads_studio_purpose').on(t.studioId,t.purpose),index('uploads_booking_purpose').on(t.bookingId,t.purpose)]);
export const guards=sqliteTable('operation_guards',{id:text('id').primaryKey(),valid:integer('valid').notNull()},t=>[check('guard_valid',sql`${t.valid} = 1`)]);
export const studioRegistry=sqliteTable('studio_registry',{id:text('id').primaryKey(),owner:text('owner'),revision:integer('revision').notNull().default(0),content:text('content').notNull()},t=>[uniqueIndex('studio_name_unique').on(sql`lower(trim(json_extract(${t.content}, '$.name')))`)]);
export const studioClaims=sqliteTable('studio_claim_requests',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),applicant:text('applicant').notNull(),status:text('status').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_claim_applicant').on(t.studioId,t.applicant)]);
export const studioVerificationRequests=sqliteTable('studio_verification_requests',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),applicant:text('applicant').notNull(),status:text('status').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_verification_pending').on(t.studioId).where(sql`${t.status} = 'pending'`)]);
export const studioStaff=sqliteTable('studio_staff',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),email:text('email').notNull(),status:text('status').notNull(),role:text('role').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_staff_email').on(t.studioId,t.email)]);
export const studioBookings=sqliteTable('studio_bookings',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),customer:text('customer').notNull(),requestKey:text('request_key').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_booking_key').on(t.customer,t.requestKey)]);
export const studioSlots=sqliteTable('studio_booking_slots',{studioId:text('studio_id').notNull(),roomId:text('room_id').notNull(),date:text('date').notNull(),minute:integer('minute').notNull(),bookingId:text('booking_id').notNull()},t=>[primaryKey({columns:[t.studioId,t.roomId,t.date,t.minute]})]);
export const studioIssues=sqliteTable('studio_issues',{id:text('id').primaryKey(),reporter:text('reporter').notNull(),content:text('content').notNull()});
export const studioAudit=sqliteTable('studio_audit',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),actor:text('actor').notNull(),event:text('event').notNull(),createdAt:text('created_at').notNull()});

export const studioRegistrations=sqliteTable('studio_registrations',{id:text('id').primaryKey(),applicant:text('applicant').notNull(),status:text('status').notNull(),content:text('content').notNull()},t=>[uniqueIndex('pending_registration_name').on(sql`lower(trim(json_extract(${t.content}, '$.name')))` ).where(sql`${t.status} = 'pending'`)]);
export const studioMembers=sqliteTable('studio_members',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),customer:text('customer').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_member_customer').on(t.studioId,t.customer)]);
export const plannerUsage=sqliteTable('planner_usage',{actor:text('actor').notNull(),day:text('day').notNull(),count:integer('count').notNull()},t=>[primaryKey({columns:[t.actor,t.day]})]);
export const billingAccounts=sqliteTable('billing_accounts',{studioId:text('studio_id').primaryKey(),content:text('content').notNull()});
export const billingCheckouts=sqliteTable('billing_checkouts',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),owner:text('owner').notNull(),provider:text('provider').notNull(),status:text('status').notNull(),createdAt:text('created_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('one_pending_checkout_per_studio').on(t.studioId).where(sql`${t.status} IN ('creating','pending')`)]);
export const billingEvents=sqliteTable('billing_events',{provider:text('provider').notNull(),id:text('id').notNull()},t=>[primaryKey({columns:[t.provider,t.id]})]);
export const studioCalendarEvents=sqliteTable('studio_calendar_events',{bookingId:text('booking_id').primaryKey(),studioId:text('studio_id').notNull(),eventId:text('event_id').notNull(),status:text('status').notNull(),updatedAt:text('updated_at').notNull(),content:text('content').notNull()},t=>[check('calendar_sync_status',sql`${t.status} IN ('synced','awaiting_setup','failed')`),uniqueIndex('calendar_event_id_unique').on(t.eventId).where(sql`${t.eventId} != ''`)]);
export const studioSettlements=sqliteTable('studio_settlements',{id:text('id').primaryKey(),bookingId:text('booking_id').notNull(),studioId:text('studio_id').notNull(),customer:text('customer').notNull(),status:text('status').notNull(),revision:integer('revision').notNull().default(0),roomSubtotal:integer('room_subtotal_cents').notNull(),addOnSubtotal:integer('addon_subtotal_cents').notNull(),gross:integer('gross_cents').notNull(),customerFee:integer('customer_fee_cents').notNull(),studioFee:integer('studio_fee_cents').notNull(),platformFee:integer('platform_fee_cents').notNull(),customerTotal:integer('customer_total_cents').notNull(),depositDue:integer('deposit_due_cents').notNull(),studioNet:integer('studio_net_cents').notNull(),currency:text('currency').notNull(),settledMonth:text('settled_month'),settledAt:text('settled_at'),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('settlement_booking_unique').on(t.bookingId),index('settlement_studio_month').on(t.studioId,t.settledMonth),index('settlement_customer_status').on(t.customer,t.status),check('settlement_status_valid',sql`${t.status} IN ('awaiting_payment','proof_submitted','payment_declined','settled_off_platform','disputed','cancelled')`),check('settlement_currency_usd',sql`${t.currency} = 'USD'`),check('settlement_integer_arithmetic',sql`${t.roomSubtotal} >= 0 AND ${t.addOnSubtotal} >= 0 AND ${t.gross} = ${t.roomSubtotal} + ${t.addOnSubtotal} AND ${t.customerFee} >= 0 AND ${t.studioFee} >= 0 AND ${t.platformFee} = ${t.customerFee} + ${t.studioFee} AND ${t.customerTotal} = ${t.gross} + ${t.customerFee} AND ${t.studioNet} = ${t.gross} - ${t.studioFee} AND ${t.depositDue} >= 0 AND ${t.depositDue} <= ${t.customerTotal}`)]);
export const settlementEvents=sqliteTable('settlement_events',{id:text('id').primaryKey(),settlementId:text('settlement_id').notNull(),studioId:text('studio_id').notNull(),actor:text('actor').notNull(),event:text('event').notNull(),idempotencyKey:text('idempotency_key').notNull(),createdAt:text('created_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('settlement_actor_idempotency').on(t.actor,t.idempotencyKey),index('settlement_events_settlement').on(t.settlementId,t.createdAt)]);
export const studioInvoices=sqliteTable('studio_invoices',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),period:text('period').notNull(),status:text('status').notNull(),gross:integer('gross_cents').notNull(),platformFee:integer('platform_fee_cents').notNull(),studioNet:integer('studio_net_cents').notNull(),issuedAt:text('issued_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('studio_invoice_period').on(t.studioId,t.period),check('studio_invoice_status',sql`${t.status} = 'issued'`),check('studio_invoice_arithmetic',sql`${t.gross} >= 0 AND ${t.platformFee} >= 0 AND ${t.studioNet} >= 0`)]);
export const feePolicies=sqliteTable('fee_policies',{id:text('id').primaryKey(),scope:text('scope').notNull(),studioId:text('studio_id'),roomId:text('room_id'),status:text('status').notNull(),effectiveFrom:text('effective_from').notNull(),effectiveUntil:text('effective_until'),revision:integer('revision').notNull().default(0),content:text('content').notNull()},t=>[index('fee_policy_effective_lookup').on(t.scope,t.studioId,t.roomId,t.status,t.effectiveFrom,t.effectiveUntil),check('fee_policy_scope_valid',sql`${t.scope} IN ('global','room')`),check('fee_policy_status_valid',sql`${t.status} IN ('active','retired')`)]);
export const feePolicyEvents=sqliteTable('fee_policy_events',{id:text('id').primaryKey(),policyId:text('policy_id').notNull(),actor:text('actor').notNull(),event:text('event').notNull(),idempotencyKey:text('idempotency_key').notNull(),createdAt:text('created_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('fee_policy_actor_idempotency').on(t.actor,t.idempotencyKey),index('fee_policy_events_policy').on(t.policyId,t.createdAt)]);
export const bookingSeries=sqliteTable('booking_series',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),customer:text('customer').notNull(),requestKey:text('request_key').notNull(),status:text('status').notNull(),createdAt:text('created_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('booking_series_customer_key').on(t.customer,t.requestKey),index('booking_series_studio_created').on(t.studioId,t.createdAt),check('booking_series_status_valid',sql`${t.status} IN ('requested','closed')`)]);
export const membershipLedger=sqliteTable('membership_ledger',{id:text('id').primaryKey(),studioId:text('studio_id').notNull(),membershipId:text('membership_id'),customer:text('customer').notNull(),bookingId:text('booking_id'),kind:text('kind').notNull(),amount:integer('amount_cents').notNull(),method:text('method'),idempotencyKey:text('idempotency_key').notNull(),occurredAt:text('occurred_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('membership_ledger_customer_key').on(t.customer,t.idempotencyKey),uniqueIndex('membership_ledger_booking_kind').on(t.bookingId,t.kind).where(sql`${t.bookingId} IS NOT NULL`),index('membership_ledger_studio_time').on(t.studioId,t.occurredAt),index('membership_ledger_membership_time').on(t.membershipId,t.occurredAt),check('membership_ledger_kind_valid',sql`${t.kind} IN ('membership_settlement','attendance','loyalty_credit','loyalty_redemption','loyalty_reversal')`),check('membership_ledger_amount_valid',sql`${t.amount} >= 0`)]);
export const loyaltyCreditSettings=sqliteTable('loyalty_credit_settings',{id:text('id').primaryKey(),enabled:integer('enabled').notNull(),credit:integer('credit_cents').notNull(),revision:integer('revision').notNull().default(0),content:text('content').notNull()},t=>[check('loyalty_credit_enabled_valid',sql`${t.enabled} IN (0,1)`),check('loyalty_credit_amount_valid',sql`${t.credit} >= 0`)]);
export const studioLoyaltyBalances=sqliteTable('studio_loyalty_balances',{studioId:text('studio_id').notNull(),customer:text('customer').notNull(),balance:integer('balance_cents').notNull(),updatedAt:text('updated_at').notNull()},t=>[primaryKey({columns:[t.studioId,t.customer]}),check('studio_loyalty_balance_valid',sql`${t.balance} >= 0`)]);
export const loyaltyCreditEvents=sqliteTable('loyalty_credit_events',{id:text('id').primaryKey(),actor:text('actor').notNull(),idempotencyKey:text('idempotency_key').notNull(),createdAt:text('created_at').notNull(),content:text('content').notNull()},t=>[uniqueIndex('loyalty_credit_actor_key').on(t.actor,t.idempotencyKey)]);
